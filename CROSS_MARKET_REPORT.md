# 舟券プール間裁定 検証レポート

**結論: 単勝プールと3連単プールの間に、統計的に有意な価格の不整合が存在する。ただし実戦投入の可否を判断する前に、暫定オッズでの再現性検証が必須。**

作成日: 2026年8月6日（2026年8月7日、標本を603→1,819レースに拡大して更新）
検証対象: 単勝・複勝・2連単・2連複・3連単・3連複の6プールの最終オッズ（ホールドアウト期間からランダム抽出した2,000レース中、突き合わせできた1,819レース）
検証データ: `odds_result_cache.csv`（3連単オッズ・実結果）× `odds_cross_cache.csv`（新規取得、他5種オッズ）

---

## 1. 動機

これまでの検証（`REPORT.md`）は、自作モデルが3連単プールの中で市場（オッズ）に対して優位性を持つかを問うものだった。結論は「持たない」だった。

本レポートは問いを変える。**同一レースに対して、賭け手は舟券の種類（プール）ごとに別々の判断をしている**。パリミュチュエル方式では舟券種ごとに資金プールが分かれるため、同じ出走表を見ていても「3連単で穴を狙う層」と「単勝で本命党の層」は異なる判断をしうる。もし一方のプールが他方より系統的に正確なら、正確な方の情報を使って不正確な方のプールに賭けることで、モデルの予測力に頼らず優位性を得られる可能性がある。

これは算数的な整合性チェックであり、「競艇の知識で市場に勝つ」話ではない。

## 2. 手法

### 2.1 データ

`odds_result_cache.csv`（ホールドアウト期間、3連単オッズ＋実結果を格納済み）から無作為に2,000レースを抽出し、`scripts/fetch_cross_odds.py` で単勝・複勝・2連単・2連複・3連複のオッズを追加取得した（`boatrace.jp` の確定後オッズページから取得）。2,000件中、他プールのオッズが揃わなかった一部を除いた1,819レースを分析対象とした。

パース精度は理論値と突き合わせて確認済み：2連単30通り（6×5）、2連複15通り（₆C₂）、3連複20通り（₆C₃）、単勝・複勝各6件。1レースで3連単オッズとの整合性も確認した（3連複の implied 確率と、対応する3連単6通りの implied 確率の合計はほぼ一致）。

### 2.2 周辺化確率

3連単オッズから「その艇が1着になる確率」を以下で算出する（三連単プールの"意見"を単勝の土俵に変換する）。

```
q_3t(艇b) = Σ_{x,y≠b} 1/odds_3t(b-x-y)  を6艇で正規化
```

単勝プールの implied 確率 `q_tan(艇b) = (1/odds_tan(b))` も同様に正規化する。両者の差 `diff = q_3t(b) - q_tan(b)` が、2つのプール間の意見の乖離である。

3連複×2連複についても同じ考え方で周辺化確率を作り、同様に検証した。

## 3. 結果

### 3.1 較正比較（どちらのプールがより正確か）

| | LogLoss（小さいほど正確） |
|---|---:|
| 単勝プール | 1.2353 |
| 3連単プール周辺化 | **1.1478** |

3連単プールの implied 確率の方が、実際の1着結果をより正確に予測している。3連単は競艇で最も資金量が大きい主戦場のプールであり、単勝は相対的に軽視されがちな脇のプールであるため、情報の集約度に差がある、というのが自然な解釈である。

### 3.2 乖離ベースの単勝ベット（オッズ帯を制御しない場合）

3連単プールが単勝プールより強気な艇（`diff`が大きい）に単勝で賭けた場合の回収率：

| 条件 | 賭数 | 的中 | 回収率 | 95%信頼区間 | 赤字確率 |
|---|---:|---:|---:|---:|---:|
| diff ≥ 0.03 | 2,441 | 950 | 118.1% | 106.5%〜130.7% | 0.1% |
| diff ≥ 0.05 | 1,752 | 797 | 118.5% | 106.9%〜131.7% | 0.0% |
| diff ≥ 0.08 | 1,134 | 602 | 120.6% | 110.7%〜130.8% | 0.0% |
| diff ≥ 0.12 | 642 | 372 | **129.2%** | 116.3%〜143.0% | 0.0% |

逆方向（単勝プールの方が強気）は明確に沈む：

| 条件 | 賭数 | 的中 | 回収率 | 95%信頼区間 |
|---|---:|---:|---:|---:|
| diff ≤ -0.03 | 2,653 | 373 | 35.9% | 31.5%〜40.5% |
| diff ≤ -0.05 | 1,585 | 275 | 35.8% | 31.0%〜40.7% |
| diff ≤ -0.08 | 822 | 175 | 37.4% | 31.9%〜43.3% |

両方向が対称に振れている。これはノイズや複数閾値のp-hackingでは説明しにくい（無関係な乱数なら片方向だけがたまたま良く見えることはあっても、逆方向まで綺麗に対称に沈むことは起きにくい）。

### 3.3 favorite-longshot biasとの切り分け（最重要の検定）

ただし `diff` が大きい舟券は、単勝オッズが低い（本命）方に偏っていた（diff≥0.08の平均オッズ3.46倍・中央値2.10倍）。本Botの過去の検証で、**低オッズ帯はそれだけで回収率が高い**（favorite-longshot bias）ことが分かっている。つまり上記の結果は「単なる本命党の言い換え」にすぎない可能性がある。

そこで、オッズ帯を固定した上で `diff` 選抜の効果だけを取り出した。1〜6倍に絞って統合すると：

| 条件 | 賭数 | 的中 | 回収率 | 95%信頼区間 |
|---|---:|---:|---:|---:|
| 1〜6倍 かつ diff≥0.08 | 972 | 567 | **111.7%** | **104.1%〜119.4%** |

**信頼区間の下限が100%を上回っている（赤字である確率0.12%）。**

さらに、「diffを見ずに同じオッズ帯から無作為に972件選んだ場合」とのプラシーボ比較を行った。

| | 回収率 |
|---|---:|
| 無作為選抜（10,000試行の平均） | 73.4%（標準偏差3.5pt） |
| diff≥0.08による選抜 | **111.7%** |

**p値 < 0.0001**（1万回の無作為試行中、実際の選抜結果を上回ったものは0件）。これは「オッズ帯を選ぶだけでは説明できない、diffそのものに情報がある」ことの直接的な証拠である。

### 3.4 頑健性チェック

| チェック項目 | 結果 |
|---|---|
| 前半/後半で分割（1〜6倍・diff≥0.08） | 前半114.2%（n=490）／後半109.1%（n=482）— 両方とも黒字 |
| 高配当上位10本を除外（同条件） | 111.7% → 106.1%（除外後も黒字を維持） |
| 帰無仮説（優位性ゼロ）に対するp値 | < 0.0001（プラシーボ検定・各diff閾値とも） |

過去4回の偽陽性（漏洩・偏った標本・小標本・in-sample汚染）のいずれのパターンにも当てはまらないことを確認している：無作為抽出した標本、期間分割で両半黒字、上位配当除外後も黒字、oddsは全て確定後の実オッズ。603→1,819レースへの標本拡大でも効果は縮まなかった（116.9%→111.7%は平均への回帰の範囲内で、信頼区間はむしろ狭まっている）。

### 3.5 参考: 2連複×3連複（非有意）

同じ考え方を2連複プールと3連複プール周辺化で試したが、こちらは有意ではなかった。

| 条件 | 賭数 | 的中 | 回収率 | 95%信頼区間 |
|---|---:|---:|---:|---:|
| diff ≥ 0.02 | 7,438 | 328 | 80.7% | 69.8%〜92.3% |
| diff ≥ 0.04 | 2,218 | 110 | 81.7% | 64.5%〜100.0% |
| diff ≥ 0.06 | 742 | 48 | 107.4% | 75.3%〜142.4% |

標本を1,819レースに拡大しても信頼区間は100%をまたいだままで、単勝×3連単ほどの強さでは再現しなかった。3連複はプール自体の資金規模が小さく、ノイズが大きいためと考えられる。**この経路は現時点では結論を出せない（黒字とも赤字とも言い切れない）。**

## 4. 留保事項（実戦投入前に必ず解消すべき点）

### 4.1 【最重要】確定後オッズで検証している

本検証は**レース確定後の最終オッズ**（両プールとも）を使っている。本Botが実際に賭けるのは締切5〜35分前であり、`model_evaluation_log.md` で既に実測されている通り、その時点の暫定オッズは最終オッズと中央値32%乖離する。

**この乖離が単勝プールと3連単プールで同じように起きるなら問題ない**（両方とも同程度にブレるだけなら、diffの符号や大きさへの影響は限定的と期待できる）。しかし、プールごとに資金の集まり方・タイミングが異なる可能性があり、**締切直前の暫定オッズでdiffが同じように機能するかは全く未検証**。これを確認せずに実戦投入するのは、st_漏洩と同じ「バックテストと本番の分布が違う」罠を繰り返すリスクがある。

→ 対策: `scripts/measure_odds_drift.py` と同様の手法で、締切前の暫定オッズでも同じ検定をやり直す必要がある。

### 4.2 サンプルサイズ（解消済み）

初版（603レース）から1,819レース（艇単位で10,914件）まで拡大した。効果は縮まらず、信頼区間はむしろ狭まった（116.9%[103.8〜130.0%] → 111.7%[104.1〜119.4%]）。本Botの他の検証（`edge_check.py`は280,800点）と比べればまだ小さいが、当面の判断には十分な規模と考える。

### 4.3 賭け方の実装は未着手

現行の`main.py`は3連単のみを買う設計になっている。単勝を買うロジック（オッズ取得・ケリー計算・Discord通知）は存在せず、新規実装が必要。

## 5. 結論

単勝プールと3連単プールの間には、favorite-longshot biasだけでは説明できない、統計的に有意な価格の不整合が存在する（1〜6倍帯・n=972でp<0.0001、信頼区間104.1%〜119.4%）。603→1,819レースへの標本拡大でも効果は縮まらず、むしろ信頼区間が狭まった。これは公開情報の解釈でモデルが市場に勝とうとした従来のアプローチとは異なり、**「2つの独立した資金プールの意見が食い違っている」という、モデルの巧拙に依存しない構造的な発見**である。

ただし、これまでの4回の偽陽性の教訓を踏まえ、**この結果だけで実戦投入すべきではない**。最大のリスクは4.1で述べた「確定後オッズと締切直前の暫定オッズの分布の違い」であり、これはまだ解消していない。次の一手は、暫定オッズでもこの効果が残るかを実測することであり、それが確認できて初めて実装を検討すべき段階だと考える。

## 6. 今後のステップ

- [x] サンプルを603→1,819レースに拡大（完了。効果は維持）
- [ ] 締切前の暫定オッズで同じdiff検定を再現する（本命・実施中: `scripts/measure_cross_market_drift.py` で当日ライブ計測を稼働中）
- [ ] 2連複×3連複経路も標本を増やして再検証する（現状は非有意）
- [ ] 上記が黒字を維持したら、単勝ベットロジックをmain.pyに実装する

使用スクリプト:
```
python scripts/fetch_cross_odds.py --source odds_result_cache.csv --limit 2000
python scripts/cross_market_arbitrage.py
python scripts/measure_cross_market_drift.py --hours 18   # レース開催時間中に稼働
python scripts/measure_cross_market_drift.py --finalize   # 終了後に集計
```
