name: Schedule Probe

on:
  schedule:
    - cron: '2-59/5 * * * *'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print schedule probe
        run: |
          date -u
          echo "schedule probe ran"
